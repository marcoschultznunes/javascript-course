/*
    You do not need to assign types to your Typescript variables, it is optional.
*/

const a: string = 'Hello There'
let b: number = 22.7
let c: number = -44
let d: boolean = false

/*
    Important: Do not use uppercase with typescript types such as String and Number, as this 
    refers to classes and not the primitive types.
*/


/*
    Now you can no longer reassign your lets to different types

    b = 'A' => would throw an error detectable by the linter.
*/

export default {}
