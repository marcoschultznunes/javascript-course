/*
    To work with nodejs libraries and typescript we need to install an additional @types library
    for each library there is one available. This will help autocompletion.

    express type:
        npm install --save @types/express
    body parser:
        npm install --save @types/body-parser
    cors:
        npm install --save @types/cors
    morgan:
        npm install --save @types/morgan

    So it's basically @types/nameofthelibrary
*/
