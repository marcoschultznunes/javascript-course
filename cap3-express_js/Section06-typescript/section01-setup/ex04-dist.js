/*
    We'll now use tsconfig.json to make our TS files compile to a separate dist folder, 
    so our development code doesn't mix with the compiled js files.

    On tsconfig.json:
        "outDir": "./dist", (uncomment)
        "rootDir": "./app", 
*/