/*
    They are:

    1 - (static) getDerivedStateFromError(error)

    2 - componentDidCatch(error, info)

    They are called when there is an error during rendering, in a lifecycle method, or in
    the constructor of a child component.

    They will be explained in a more appropriate section for them
*/