/*
    Lazy loading is when, instead of loading the entire app in one go, we instead load each 
    component when it is necessary, to optimize performance by not loading components that the 
    user won't encounter.
*/
