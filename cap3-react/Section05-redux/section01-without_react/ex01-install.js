/*
    Redux is a JAVASCRIPT library used to manage the application state in a predictable way.

    This first section will cover redux usage without react, in order to explain how it works.
*/

/*
    First, we are going to make a project folder and do "npm init -y", to start a node project,
    then we'll install redux with the following command:
        npm install --save redux
*/
