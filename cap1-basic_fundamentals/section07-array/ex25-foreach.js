const nums = [1, 2, 4, 8, 16, 32, 64]

nums.forEach(num => console.log(num)) //foreach with no index


console.log()


const users = ['Jãozão', 'Régis Darégis', 'Abel Caim', 'Zé do Karalho'] 

users.forEach((user, index) => console.log(`${index}) - ${user}`)) //doing it with indexes