let biggerNumber = 3 > 4 ? 3 : 4 //3 > 4 => false => 4 is assigned

//value to the left: true, to the right: false

console.log(biggerNumber)

/**
 *  Ternary ops beat if/else on performance, therefore are useful.
 * 
 *  But its best to be careful not to complicate your code
 * 
 *  a ternary inside a ternary can be quite difficult to understand and code polluting
*/